type Query {
    create_new_student(): Student
    fetch_student_details(_id: ID!): Student
    add_class(): Student : Class
    fetch_classes_details (_id: ID!): Class
}

type Student {
    id: ID!
    name: String!
}

type Class {
    id: ID!
    name: String!
    student: [Student]
}